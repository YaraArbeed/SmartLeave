@{
    ViewData["Title"] = "Admin Dashboard";
    Layout = "_Layout";
}

<div class="container-fluid px-4 py-4">
    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3 mb-0 text-gray-800">Admin Dashboard</h1>
        <div class="d-flex gap-2">
            <button class="btn btn-light rounded-circle" title="Notifications">
                <i class="fas fa-bell"></i>
            </button>
            <button class="btn btn-light rounded-circle" title="Settings">
                <i class="fas fa-cog"></i>
            </button>
            <button class="btn btn-light rounded-circle" title="Logout">
                <i class="fas fa-sign-out-alt"></i>
            </button>
        </div>
    </div>

    <!-- Welcome Section -->
    <div class="mb-4">
        <h2 class="h4 mb-1">Welcome back, Admin! 👋</h2>
        <p class="text-muted">Here's what's happening with your platform today.</p>
    </div>

   